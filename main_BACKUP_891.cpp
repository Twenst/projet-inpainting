#include "utils.h"
#include "Inpainter.h"

int main()
{

    //Hello
    //Salut

    //à faire :
    //créer les objets(image IMG (image), zone à remplacer ZR (image), zone à copier ZC (image), bordure B (bordure = classe avec tableau et priorité))
    //définir les paramètres (taille de la zone autour des points t=5 pixels (const int))
    //fonction qui définit la bordure (en cliquant sur l'image) Selection (fonction qui à partir des clicks les stocke dans une bordure)
    //fonction qui efface la zone dans la bordure Delete (prend en argument une image et la renvoie blanche)
    //fonction qui calcule les priorités des points sur la bordure Priority (prend une bordure et la modifie)
    //fonction qui cherche la zone la plus similaire Similarity (prend 2 images et en renvoie une)
    //fonction qui recopie la zone Recopy (prend 2 images et en renvoie 1)
    //fonction qui update la bordure et les priorités Update (prend en argument une bordure et la modifie)

    return 0;
}
